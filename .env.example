# 新しい Mac のセットアップ時にこのファイルをコピーして使います。
# cp .env.example .env
# .env はGit管理外（.gitignore済み）

# ── Raycast ──────────────────────────────────────────────
# Raycast > Settings > General > Export Password で設定したパスワード
RAYCAST_EXPORT_PW=

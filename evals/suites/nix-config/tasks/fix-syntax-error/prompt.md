カレントディレクトリの `services.nix` が Nix として評価できず、`darwin-rebuild` が失敗しています。

構文エラーを修正してください。

制約:
- 設定の意味（各サービスの有効/無効やパッケージ指定）は変えないこと
- 構文エラーの修正以外の変更（整形・リネーム・追加）はしないこと

完了したら何が原因だったかを一行で報告してください。
確認や許可を求めず、完了まで自律的に進めてください。

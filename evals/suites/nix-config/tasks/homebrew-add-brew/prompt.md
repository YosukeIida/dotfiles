カレントディレクトリの `homebrew.nix` を編集して、`brews` リストに `"ripgrep"` を追加してください。

制約:
- `brews` のアルファベット順を保つこと
- `casks` には一切手を加えないこと
- ファイルは Nix として評価可能なまま保つこと

完了したら変更内容を一行で報告してください。
確認や許可を求めず、完了まで自律的に進めてください。

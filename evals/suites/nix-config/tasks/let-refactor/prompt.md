カレントディレクトリの `paths.nix` では、`/Users/yosuke/.config` というプレフィックスが複数箇所に重複して書かれています。

このプレフィックスを `let` 束縛（例: `configDir`）に括り出して、重複を解消するリファクタをしてください。

制約:
- ファイルの評価結果（各属性の値）は一切変えないこと
- 修正後、リテラル文字列 `/Users/yosuke/.config` がファイル中に1回だけ現れるようにすること

完了したら変更内容を一行で報告してください。
確認や許可を求めず、完了まで自律的に進めてください。

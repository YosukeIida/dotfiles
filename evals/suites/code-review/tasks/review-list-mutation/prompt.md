カレントディレクトリの `queue.py` をレビューしてください。

報告するのは**動作結果に影響する致命的なバグ**（ロジックエラー・データ破壊・例外）だけです。
スタイル・命名・軽微な改善提案は報告しないでください。

出力形式（厳守。この形式以外の文章は一切出力しないこと）:
- バグがあれば、各バグを次の形式で1行ずつ出力: `BUG: <ファイル名>:<関数名> <一行説明>`
- バグがなければ `NO_BUGS` とだけ出力

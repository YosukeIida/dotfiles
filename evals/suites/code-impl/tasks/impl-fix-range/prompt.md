カレントディレクトリの `ranges.py` の `parse_range` が仕様どおりに動かず、`test_ranges.py` のテストが失敗しています。

`ranges.py` を修正してテストをすべて通してください。

制約:
- `test_ranges.py` は変更しないこと
- 入力は正の整数のみ（負数や空文字列は考慮不要）
確認や許可を求めず、完了まで自律的に進めてください。

カレントディレクトリの `users.py` では、メールアドレスの正規化処理（strip + 小文字化 + ドメイン部の検証）が2つの関数に重複して書かれています。

この重複を `_normalize_email(email)` というモジュール内ヘルパー関数に抽出してください。

制約:
- ヘルパー名は `_normalize_email` とすること
- 外部から見た挙動（各関数の入出力・例外）は一切変えないこと
- `test_users.py` は変更しないこと
確認や許可を求めず、完了まで自律的に進めてください。

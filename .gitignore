.DS_Store
.env
.direnv/
.envrc
!templates/**/.envrc
result
result-*

# Raycast raw exports contain local history and device-specific data.
raycast/*.rayconfig

# Karabiner writes runtime files into ~/.config/karabiner (= symlinked repo/karabiner).
# Track only karabiner.json and assets/complex_modifications/*.json; ignore the noise.
karabiner/automatic_backups/
karabiner/*.bak.*
karabiner/assets/**/*.bak.*

# settings.api.json is generated by gen-api-settings.sh from settings.json +
# api-mode-overlay.json on every darwin-switch; tracking it just creates noise diffs.
claude/settings.api.json
evals/results/

# Orca（stablyai/orca）は agent hook を注入するたびに書き換え前の内容を .bak として
# 隣に残す。実体の変更は tracked 側の diff で見えるため、バックアップは追跡しない。
claude/settings.json.bak
codex/hooks.json.bak
